# Exact pins. Renovate (renovate.json) proposes a bump only once the release
# has been public for 7 days, which limits exposure to a hijacked release.
pyyaml==6.0.2
markdown-it-py==3.0.0
python-frontmatter==1.1.0
networkx==3.4.2
# networkx needs these for pagerank and spring_layout.
numpy==2.2.4
scipy==1.17.1
